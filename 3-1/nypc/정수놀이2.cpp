#include <stdio.h>
#include <string.h>
using namespace std;

int dp[100000000]={};
int s, e;
void gos(){
	memset(check, 0, sizeof(check));
	scanf("%d %d", &s, &e);
	
}

int main(){
	int t;
	scanf("%d", &t);
	for(int i=0;i<t;i++) gos();
	return 0;
}
