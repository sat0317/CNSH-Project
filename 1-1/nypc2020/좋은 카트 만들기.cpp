#include <stdio.h>
#define ca(i, j) ((long double)(n[0][i]+m[0][j])/(n[1][i]+m[1][j]))
#define nca(i, j) ((long double)(n[0][i]+m[0][j])/imsi[j])
int n[2][300001], m[2][300001];
int a, b, bi, i, j, k;
long double f;



int main(){
    scanf("%d %d", &a, &b);
    for(i=0; i<a; i++) scanf("%d %d", &n[0][i], &n[1][i]);
    for(i=0; i<b; i++) scanf("%d %d", &m[0][i], &m[1][i]);
    for(i=0; i<a; i++){
	    	switch(b){
	    		
			}
        printf("%d\n", k+1);
        f=0;
    }
    return 0;
}
